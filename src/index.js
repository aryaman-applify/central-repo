document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  root.innerHTML = "<h1>Hello Aryaman!</h1><p>This is built with Webpack and deployed via CodeBuild → EC2 → Nginx.</p>";
});
